services:
  - type: web
    name: nutricoach-api
    env: node
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: PORT
        value: 10000
